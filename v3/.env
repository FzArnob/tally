# Base URL of the PHP API, relative to the web server root.
# XAMPP default: the project sits at http://localhost/tally/ so the API is at /tally/api/.
# Change this if you deploy the project under a different path.
VITE_API_BASE=/tally/api/
